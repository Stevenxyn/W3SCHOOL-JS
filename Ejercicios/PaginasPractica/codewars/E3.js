//Write a function which converts the input string to uppercase.

function makeUpperCase(str) {
}

//solution exercise 
function makeUpperCase(str) {
    return str.toUpperCase()
}
console.log(makeUpperCase("Aprendiendo Javascript!"));

//solution with arrow function
const makeUpperCaseTwo = (str) => str.toUpperCase();
console.log(makeUpperCaseTwo("Solucion desde un arrow function"));